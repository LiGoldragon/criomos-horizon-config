# criomos-horizon-config

Pan-horizon NOTA configuration for the LiGoldragon CriomOS horizon.

`horizon-rs` reads `horizon.nota` together with a cluster `datom.nota`
to derive projected Horizon JSON for CriomOS and CriomOS-home.
